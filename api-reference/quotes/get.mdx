---
title: "Get Quote"
openapi: "GET /organizations/{organization_id}/quotes/{quote_id}"
description: "Retrieve details for a specific quote"
---

Retrieve details for a specific quote.

## Common Headers

| Header          | Value              | Required | Description                     |
| :-------------- | :----------------- | :------- | :------------------------------ |
| `Authorization` | `Bearer <api-key>` | true     | Your API key for authentication |
| `Content-Type`  | `application/json` | true     | Content type of the request     |

## Path

<ParamField path="organization_id" type="string" required>
  ID of organization
</ParamField>

<ParamField path="quote_id" type="string" required>
  ID of quote
</ParamField>

## Code examples

<CodeGroup>
  ```bash curl
  curl -X GET "https://api-sandbox.stablesea.com/v1/organizations/{organization_id}/quotes/{quote_id}" \
    -H "Authorization: Bearer YOUR_API_KEY" \
    -H "Content-Type: application/json"
  ```
  ```go Go
  package main

  import (
      "context"
      "github.com/stablesea/sdk-go"
  )

  func main() {
      client := sdk.NewClient("YOUR_API_KEY", "")
      quote, err := client.GetQuote(context.Background(), "org_01k2cm4r59e5z8k5ggrbbxjcwy", "qt_01k4qpmvgxfsg9db9mjrtq9tk2")
      if err != nil {
          panic(err)
      }
      _ = quote
  }
  ```
  ```typescript TypeScript
  import { createClient } from '@stablesea/sdk';

  const client = createClient({ apiKey: 'YOUR_API_KEY' });
  const quote = await client.quotes.get(
    'org_01k2cm4r59e5z8k5ggrbbxjcwy',
    'qt_01k4qpmvgxfsg9db9mjrtq9tk2'
  );
  ```
  ```python Python
  from stablesea import StableseaClient

  client = StableseaClient(api_key="YOUR_API_KEY")
  quote = client.quotes.get(
      organization_id="org_01k2cm4r59e5z8k5ggrbbxjcwy",
      quote_id="qt_01k4qpmvgxfsg9db9mjrtq9tk2",
  )
  client.close()
  ```
</CodeGroup>
