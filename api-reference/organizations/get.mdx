---
title: "Get Organization"
openapi: "GET /organizations/{organization_id}"
description: "Retrieve details for a specific organization"
---

Retrieve details for a specific organization.

## Common Headers

| Header          | Value              | Required | Description                     |
| :-------------- | :----------------- | :------- | :------------------------------ |
| `Authorization` | `Bearer <api-key>` | true     | Your API key for authentication |
| `Content-Type`  | `application/json` | true     | Content type of the request     |

## Path

<ParamField path="organization_id" type="string" required>
  ID of organization to retrieve
</ParamField>

## Code examples

<CodeGroup>
  ```bash curl
  curl -X GET "https://api-sandbox.stablesea.com/v1/organizations/{organization_id}" \
    -H "Authorization: Bearer YOUR_API_KEY" \
    -H "Content-Type: application/json"
  ```
  ```go Go
  package main

  import (
      "context"
      "github.com/stablesea/sdk-go"
  )

  func main() {
      client := sdk.NewClient("YOUR_API_KEY", "")
      org, err := client.GetOrganization(context.Background(), "org_01k2cm4r59e5z8k5ggrbbxjcwy")
      if err != nil {
          panic(err)
      }
      _ = org
  }
  ```
  ```typescript TypeScript
  import { createClient } from '@stablesea/sdk';

  const client = createClient({ apiKey: 'YOUR_API_KEY' });
  const org = await client.organizations.get('org_01k2cm4r59e5z8k5ggrbbxjcwy');
  ```
  ```python Python
  from stablesea import StableseaClient

  client = StableseaClient(api_key="YOUR_API_KEY")
  org = client.organizations.get(organization_id="org_01k2cm4r59e5z8k5ggrbbxjcwy")
  client.close()
  ```
</CodeGroup>
